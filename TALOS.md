# Rules for agents working in this repo 📜

- Run `python -m pytest tests/ -q` before claiming any change works.
- Keep modules small and heavily commented — this repo is a teaching
  codebase; clarity beats cleverness.
- Never commit `.env` or anything under `.talos/sessions/`.
