# OpenAI (or compatible API)
OPENAI_API_KEY=sk-...

# Model to use for the agent
TALOS_MODEL=gpt-4o-mini

# Optional: override the default system prompt file
# TALOS_SYSTEM_PROMPT_PATH=src/talos/prompts/system.md
